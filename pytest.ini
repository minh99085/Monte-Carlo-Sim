[pytest]
# deploy/ carries files installed onto other systems (e.g. the Robinhood
# bridge's test file, which imports the bot's packages) — never collect there.
norecursedirs = deploy .git outputs calibration
